<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Llascer SIMBA{% endblock %}</title>
        <!-- Thème Bootstrap -->
        <link rel="stylesheet" href="https://bootswatch.com/5/sketchy/bootstrap.rtl.min.css">
        <!-- Font Awesome pour les icônes -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
        
        <style>
            body {
                background-image: url('{{ asset('Fond.webp') }}');
                background-size: cover; /* Adapte l'image à la taille de l'écran */
                background-repeat: no-repeat; /* Empêche la répétition */
                background-attachment: fixed; /* Rend le fond fixe lors du défilement */
                background-position: center; /* Centre l'image */
            }
            .text-container {
                background-color: rgba(255, 255, 255, 0.3); /* Blanc translucide */
                border-radius: 10px; /* Coins arrondis */
                padding: 20px; /* Espacement interne */
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Légère ombre pour l'effet visuel */
                margin-bottom: 20px; /* Espacement entre les blocs */
            }
        </style>

        {% block stylesheets %}
        {% endblock %}
    </head>
    <body>
        <!-- Bouton vers la page d'accueil -->
        <div class="position-fixed top-0 start-0 m-3">
            <a href="{{ path('app_home') }}" class="btn btn-primary">
                <i class="fas fa-home"></i> Accueil
            </a>
        </div>

        {% block body %}{% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}

        <!-- Footer -->
        <footer class="bg-dark text-white text-center py-4 mt-5">
            <!-- Droits d'auteur -->
            <p>&copy; 2025 Llascer Simba. Tous droits réservés.</p>
            <p>
                Ce site est protégé par le droit d'auteur. Toute reproduction ou utilisation des contenus (textes, images, code) est strictement interdite sans autorisation préalable.
            </p>

            <!-- Crédits pour les ressources libres -->
            <p>
                Images et contenus graphiques issus de ressources libres de droits :
                <a href="https://www.istockphoto.com/" target="_blank" class="text-warning">iStock</a>
            </p>

            <!-- Réseaux sociaux -->
            <div class="d-flex justify-content-center mt-3">
                <a href="https://www.instagram.com/llasc_simba/" target="_blank" class="btn btn-danger btn-sm mx-1">
                    <i class="fab fa-instagram"></i> Instagram
                </a>
            </div>
        </footer>
    </body>
</html>
